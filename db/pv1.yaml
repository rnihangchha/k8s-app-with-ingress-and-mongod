apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-pv-1
  namespace: pymongo
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/db1
